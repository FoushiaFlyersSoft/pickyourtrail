import React from 'react';
import { Row, Col, Button } from 'antd';
import { useHistory } from 'react-router-dom';

import LottieComponent from '@sharedComponent/lottie-component';
import LottieFile from 'src/assets/lottie-files';

import './welcome-page.scss';
import Draggable from 'react-draggable';
import { Paper } from '@material-ui/core';
import GalleryList from '@sharedComponent/gallery/gallery';
import Grid from 'antd/lib/card/Grid';
import SideBlock from '@sharedComponent/sideblock/sideblock';
import MainBlock from '@sharedComponent/mainblock';

const Welcome = () => {
	const history = useHistory();
	return (
		<>
		
		
		
		</>
	);
};

export default Welcome;
